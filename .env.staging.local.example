# Copy this file to .env.staging.local and fill in the real staging values.
# This is used for running db migrations against the staging database, e.g. `pnpm db:migrate:staging`
DATABASE_URL=
DATABASE_URL_UNPOOLED=
