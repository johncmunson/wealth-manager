# Copy this file to .env.production.local and fill in the real production values.
# This is used for running db migrations against the production database, e.g. `pnpm db:migrate:prod`
DATABASE_URL=
DATABASE_URL_UNPOOLED=
