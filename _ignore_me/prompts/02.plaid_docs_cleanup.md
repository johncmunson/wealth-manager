This is the idea that I want to build: @prompts/01.initial_idea.md

Currently, this repo is essentially just the vanilla boilerplate that you get with create-next-app. The main thing that I've been working on adding is the docs in the `_context` folder so that when I actually get started on building, coding agents will have easy access to the context that they need.

However, the `_context/plaid/` folder is a little bloated right now. Your task is to clean this up using parallel worker subagents (max 8 at a time).

## Your Instructions

1. Launch a worker subagent for every file in the plaid folder
2. Each worker subagent is to determine the following things...
   - Analyze the links in the file and delete any that are irrelevant to the idea that I want to build. Also delete links that are only partially relevant, but provide very little value.
   - If the worker agent is unsure if a link is relevant and would like to use curl to read the actual contents, it may do so.
   - If there are no links in the file worth keeping at all, the entire file may be deleted.
3. Once all subagents have finished running, the document ordering is likely to be messed up. Fix this so that the filenames again read like `01.foo.md`, `02.bar.md`, etc. with no gaps.
