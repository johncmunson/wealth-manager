 1. Use a worker subagent to inspect all of the mockups in the `_mockups` folder and then plan out a list of components that need to be built. The subagent should assume that we are going to use shadcn/ui, so it should study up on this before creating the finalized list of components. It should also study up on Next.js and/or React component best practices. For each component that is needed, write a numbered file to `planned_components/` describing how to build it, where it should be saved to in the `components/` folder, which mockups it is shown in, desired behavior and functionality, test plan, etc. e.g. `planned_components/01.investments-searchbar.md`. No backend functionality has been built out yet so, in general, if components need data or need to post data, then data or callbacks should be passed in via props. Don't actually implement or plan any backend functionality.
 2. Once step 1 is finished, launch parallel worker subagents (max 8 at a time) to implement each of the planned components.
 3. Once all components have been implemented, create a `/kitchen-sink` frontend route and add all of the newly built components to it so that I can preview them.